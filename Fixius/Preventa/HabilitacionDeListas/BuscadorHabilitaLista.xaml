<UserControl x:Class="Inteldev.Fixius.Preventa.HabilitacionDeListas.BuscadorHabilitaLista"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Margin="10" Width="400">
    <DataGrid 
        ItemsSource="{Binding Items}"
        SelectedItem="{Binding ItemSeleccionado}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Lista" Binding="{Binding Lista}"></DataGridTextColumn>            
            <DataGridTextColumn Header="Empresa" Binding="{Binding Empresa}"></DataGridTextColumn>
            <DataGridTextColumn Header="Sucursal" Binding="{Binding Sucursal}"></DataGridTextColumn>
            <DataGridTextColumn Header="División" Binding="{Binding DivisionComercial}"></DataGridTextColumn>
            <DataGridTextColumn Header="Grupo" Binding="{Binding GrupoCliente}"></DataGridTextColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
